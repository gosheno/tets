version: '3.8'

services:
  app:
    image: golang:1.21
    container_name: go_app
    working_dir: /app
    volumes:
      - ./project:/app
      - ./.env:/app/.env
    command: go run main.go
    env_file:
      - .env