
services:
  app:
    build:
      context: ./project
      dockerfile: dockerfile
    container_name: go_app
    working_dir: /app
    volumes:
      - ./project:/app
      - ./.env:/app/.env
    command: go run main.go
    env_file:
      - .env